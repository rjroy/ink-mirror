import type { Profile } from "@ink-mirror/shared";
interface ProfileEditorProps {
    initialProfile: Profile & {
        markdown: string;
    };
}
export declare function ProfileEditor({ initialProfile }: ProfileEditorProps): any;
export {};
//# sourceMappingURL=profile-editor.d.ts.map